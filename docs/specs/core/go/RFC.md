# go-stream RFC mirror

The canonical implementation spec for this module is [`docs/RFC.md`](/workspace/docs/RFC.md).
Keep this path as a compatibility mirror for agents and tooling that expect the
`docs/specs/core/go/RFC.md` location.
